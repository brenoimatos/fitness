[build]
  publish = "."
  command = ""
  base = ""

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
